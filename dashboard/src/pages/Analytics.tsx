export default function Analytics() {
  return (
    <div className="glass-panel" style={{ padding: '3rem', textAlign: 'center', marginTop: '2rem' }}>
      <h2 style={{ marginBottom: '1rem', fontSize: '2rem' }}>Analytics Dashboard</h2>
      <p style={{ color: 'var(--text-secondary)' }}>
        This page will show real-time metrics and the Recent Deliveries data table (Phase 4).
      </p>
    </div>
  );
}
